# LibraryProject

This is an initial Django project created to understand the basic structure of a Django application.

It includes default Django configuration files, an SQLite database, and a running development server.

